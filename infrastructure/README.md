# Infrastructure

This directory is reserved for deployment manifests and environment-specific infrastructure configuration.

Infrastructure will be added only after the Phase 1 deployment path is confirmed. Local orchestration lives in the repository-level `docker-compose.yml`.

